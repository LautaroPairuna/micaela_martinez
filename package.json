{
  "name": "mica_pestanas",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "build:api": "turbo run build --filter=@app/api",
    "build:web": "turbo run build --filter=@app/web",
    "start:api": "turbo run start --filter=@app/api",
    "start:web": "turbo run start --filter=@app/web",
    "db:migrate": "turbo run db:migrate --filter=@app/api",
    "db:generate": "turbo run db:generate --filter=@app/api",
    "postinstall": "turbo run db:generate --filter=@app/api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  }
}
